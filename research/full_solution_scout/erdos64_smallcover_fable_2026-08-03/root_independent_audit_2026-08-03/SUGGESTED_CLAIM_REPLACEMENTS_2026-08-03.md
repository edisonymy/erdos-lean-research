# Suggested replacements for claims in the Fable #64 packet

These are proposed replacement blocks.  They are intentionally stored
beside, rather than applied to, the original append-only research record.

## 1. `HANDOVER.md`

### Replace items 1 and 2 under "What is proven / certified"

```markdown
1. **Certified part-size theorem.** No linear hypergraph on `sigma <= 18`
   points with point-degrees at least 3 and edge sizes at least 3 has a
   `C8`-free incidence graph. Equivalently, every bipartite graph of
   minimum degree at least 3 with no `C4` and no `C8` has at least 19
   vertices in each bipartition class. The independent successor audit
   replayed packet certificates for `sigma = 5,...,13`, used the elementary
   hand argument at `sigma = 4`, and freshly regenerated and
   `drat-trim`-verified `sigma = 14,...,18`. The fresh large artifacts must
   still be moved from the temporary audit directory into a durable archive
   before a public reproducibility claim. This part-size theorem may be a
   useful strengthening, but its exact priority remains under review.

2. **Known `sigma = 19` witness, independently recovered.**
   `sigma19_model.json` is the known graph `F038A`: Gordon Royle posted its
   full adjacency list on MathOverflow in 2009, and Erskine--Griggs--Širáň
   (2021, DOI 10.1002/jcd.21773) identify it as the Levi graph of the cyclic
   `19_3` configuration generated by `{0,1,8}`. The packet model is exactly
   isomorphic to Royle's graph. It is connected, cubic, bipartite 19+19,
   `C4`-free and `C8`-free; it has 19 unoriented `C6`s and has both a `C16`
   and a `C32`. The minimum total order 38 was already implied by the
   McKay--Afzaly exact extremal table (`ex_bip(37,{C4,C8})=54`,
   `ex_bip(38,{C4,C8})=57`), so neither the witness nor the order threshold
   is new.
```

### Replace the "Ready-to-post public update" paragraph

```markdown
## Public-update hold and correction

Do not post the existing `ISSUE_UPDATE_DRAFT.md` unchanged. An independent
priority audit found that the order-38 witness is Royle's 2009 graph
`F038A`, and the McKay--Afzaly extremal table already records enough exact
data to imply the minimum total order 38. Use the corrected replacement in
`root_independent_audit_2026-08-03/SUGGESTED_CLAIM_REPLACEMENTS_2026-08-03.md`.
Any update must distinguish the potentially stronger certificate-backed
part-size theorem from the prior total-order threshold, cite all prior
sources, say that no #64 solution was found, and link a durable archive of
the fresh `sigma = 14,...,18` certificate pairs before calling the result
reproducible.
```

### Replace the first "Unfinished small thread"

```markdown
- The `sigma = 19` object is no longer an identification task. It is
  exactly the known Foster-census graph `F038A`, the Levi graph of the
  cyclic `19_3` configuration generated by `{0,1,8}` modulo 19. The exact
  isomorphism to Royle's 2009 adjacency list is recorded in
  `root_independent_audit_2026-08-03/royle_2009_isomorphism.json`.
```

## 2. `REPORT.md`

### Replace the opening status paragraph

```markdown
**Status: no counterexample and no universal proof; no solution claim.**
This report records finite search reductions, bounded exclusions, and a
certificate-backed part-size theorem for bipartite `{C4,C8}`-free graphs.
An independent successor audit corrected a priority error: the 38-vertex
model and the minimum total-order threshold 38 are prior, not new. The
model is the known Foster-census graph `F038A`, posted explicitly by Gordon
Royle in 2009. The exact total-order threshold is also implied by the
McKay--Afzaly public extremal table. The potentially new contribution is
the stronger assertion that each bipartition class has at least 19
vertices, subject to final priority clearance and durable certificate
archiving. See
`root_independent_audit_2026-08-03/ROOT_INDEPENDENT_AUDIT_2026-08-03.md`.
```

### Replace section 2.1

```markdown
### 2.1 Certified part-size theorem; known total-order corollary

**Theorem 1.** For `sigma <= 18` there is no linear hypergraph on `sigma`
points with all point-degrees at least 3 and all edge sizes at least 3
whose incidence graph is `C8`-free. Equivalently, every bipartite graph
with minimum degree at least 3 and no `C4` and no `C8` has at least 19
vertices in each side.

Evidence: the elementary `sigma = 4` pair-budget argument; independently
replayed CNF/DRAT pairs for `sigma = 5,...,13`; and freshly regenerated,
manifest-matched where applicable, ASCII-DRAT certificates for
`sigma = 14,...,18`, all returning `s VERIFIED`. Exact dimensions and
hashes are in the independent audit. The graph-to-hypergraph reduction,
static quadrilateral clauses, and DoubleLex symmetry breaking received a
separate hostile semantic audit.

The corollary that the minimum total order is 38 is not new. Gordon Royle
posted the order-38 witness in 2009; Erskine--Griggs--Širáň identify it as
`F038A`; and the McKay--Afzaly exact extremal table already gives 54 as the
maximum edge count at order 37 and 57 at order 38. The campaign's
potential contribution is the stronger per-side bound and independent
certificate package, pending priority clearance and durable archiving.
```

### Replace items 2 and 3 under "What this changes for the campaign"

```markdown
2. In the bipartite pure-incidence class, every bipartition side has size
   at least 19. This is stronger than, but must not be confused with, the
   already-known minimum total order 38. The general small-cover case with
   core edges remains separate.
3. The finite computation supplies a reproducible route to the stronger
   part-size bound. It does not supply a new 38-vertex construction or a
   new total-order extremal value. The priority and publication task is to
   determine whether the exact per-side statement is prior and, if not,
   archive the complete certificate ladder.
```

## 3. `ISSUE_UPDATE_DRAFT.md`

### Replace the opening paragraph

```markdown
This update reports a bipartite two-defect reduction, bounded exclusions,
and an independently audited certificate-backed part-size theorem. It also
corrects a priority mistake in the internal handover: the 38-vertex graph
and the minimum total-order threshold 38 are prior. **No counterexample and
no universal proof were found; this is not a solution to #64.**
```

### Replace section 3 in full

```markdown
**3. Bipartite `{C4,C8}`-free part-size theorem and priority correction.**
The SAT encoding rules out a linear hypergraph on at most 18 points with
point-degrees at least 3, edge sizes at least 3, and `C8`-free incidence
graph. Via the exact neighborhood-hypergraph reduction, every bipartite
graph of minimum degree at least 3 with no `C4` and no `C8` therefore has
at least 19 vertices in each bipartition class. The independent audit used
a hand proof at `sigma = 4`, replayed exact CNF/DRAT pairs at
`sigma = 5,...,13`, and freshly regenerated and `drat-trim`-verified
`sigma = 14,...,18`. [Insert durable archive DOI and the complete hash
manifest here before posting this as a reproducible result.]

The minimum *total* order 38 is not a new result of this campaign. Gordon
Royle posted the explicit 38-vertex cubic bipartite graph with no `C4` or
`C8` in 2009. It is the Foster-census graph `F038A`, the Levi graph of the
cyclic `19_3` configuration generated by `{0,1,8}` (Erskine--Griggs--Širáň,
2021, DOI 10.1002/jcd.21773). The campaign model is exactly isomorphic to
Royle's graph; it has 19 unoriented 6-cycles, not 38, and it contains both
`C16` and `C32`. The McKay--Afzaly exact extremal table already records
`ex_bip(37,{C4,C8})=54` and `ex_bip(38,{C4,C8})=57`, which implies the
order-38 threshold by the minimum-degree edge count. The potentially new
content is only the stronger *per-side* lower bound, and priority for that
exact statement is still being checked.

Sources:
- Royle 2009: https://mathoverflow.net/questions/966/looking-for-cubic-bipartite-graphs-with-girth-at-least-six-and-no-cycles-of-len
- Erskine--Griggs--Širáň 2021: https://doi.org/10.1002/jcd.21773
- McKay--Afzaly extremal data: https://users.cecs.anu.edu.au/~bdm/data/extremal.html
```

### Replace the closing verification/priority paragraph

```markdown
Verification pipeline: independent standard-library graph checker; exact
isomorphism check against Royle's published adjacency list; small-case
exhaustive tests of the static `C8` predicate; independent audit of
DoubleLex; exact CNF/DRAT replay at every rung through `sigma = 18`; and
explicit hashes for the model, formulas, proofs, generator, and checker.
The order-38 novelty claim was withdrawn after the priority audit found
Royle 2009, `F038A`, and the McKay--Afzaly extremal table. Priority for the
stronger bipartition-side theorem remains search-relative until checked
with specialists.
```

## 4. Short public wording if the certificate archive is not ready

If a prompt correction must be posted before the Zenodo deposit exists,
use only this narrower wording:

```markdown
Correction: our 38-vertex model is not new. It is exactly Gordon Royle's
2009 graph `F038A`; the public McKay--Afzaly extremal table also already
implies that 38 is the minimum total order. Our model has 19 unoriented
simple 6-cycles; the earlier value 38 used an undocumented convention. We have separately
verified a stronger computational per-side exclusion through side size 18,
but we are withholding a result claim until the full certificate bundle is
deposited and the exact part-size statement receives a targeted priority
check. This remains progress on a restricted class, not a solution to
Erdős problem #64.
```
